Objectives:

● Develop code to train a machine model.

● Assess the accuracy of a machine model.

The model should predict whether a given sequence of Carb input values represents a meal or a no meal data.
